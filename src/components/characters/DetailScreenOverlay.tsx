import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const DetailScreenOverlay = () => {
  return (
    <View>
      <Text>DetailScreenOverlay</Text>
    </View>
  );
};

export default DetailScreenOverlay;

const styles = StyleSheet.create({});
